<Window x:Class="ExcelShSy.Ui.SettingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExcelShSy.Localization;assembly=ExcelShSy.Localization"
        mc:Ignorable="d"
        Title="{local:Loc SettingWindow.Title}" Height="450" Width="300" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Margin="10" >
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Margin" Value="0,0,0,10"/>
                </Style>
                <Style TargetType="StackPanel">
                    <Setter Property="Margin" Value="0,0,0,10"/>
                </Style>
            </StackPanel.Resources>


            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top">
            <TextBlock Text="{local:Loc SettingWindow.Language}" Height="Auto" VerticalAlignment="Center" />
            <ComboBox x:Name="CBLanguagues" Height="Auto" Width="150" Margin="10 0" SelectionChanged="CBLanguagues_SelectionChanged" />
        </StackPanel>

        <Button Grid.Row="0" VerticalAlignment="Top" HorizontalAlignment="Left" Content="{local:Loc SettingWindow.UserShopManager}" Padding="10 2" Click="ShopManagerOpen_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" >
            <Button Content="{local:Loc SettingWindow.ApplyButton}" Width="80" Height="Auto" Click="Apply_Click" />

        </StackPanel>
    </Grid>
</Window>
