<Window x:Class="ExcelShSy.EditLoadFilesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExcelShSy.Ui.Localization"
        xmlns:resx="clr-namespace:ExcelShSy.Ui.Resources"
        mc:Ignorable="d"
        Title="{local:Loc EditLoadFilesWindow.Title}" SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen" 
        ResizeMode="CanMinimize"
        MinWidth="250" MinHeight="400" MaxHeight="400" Width="300" MaxWidth="300">

    <Window.Resources>
        <Style x:Key="NoBorderCellStyle" TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="LightGray"/>
        </Style>
    </Window.Resources>

    <Grid Background="White" Margin="0 0 0 10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TabControl Grid.Row="0" x:Name="FilesControl" Grid.ColumnSpan="2">
            <TabItem Header="{local:Loc EditLoadFilesWindow.TargetFiles}" x:Name="Target">
                <Grid >
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="0,3">
                        <Button Padding="5 0" Tag="Target" Click="RemoveFile_Click" Content="{local:Loc EditLoadFilesWindow.Delete}" />
                        <Button Padding="5 0" Tag="Target" Click="AddFile_Click" Margin="2 0" Content="{local:Loc EditLoadFilesWindow.AddFile}" />
                    </StackPanel>

                    <DataGrid IsReadOnly="False" Grid.Row="1" HeadersVisibility="Column" ItemsSource="{Binding TemperaryTargetFiles}" d:ItemsSource="{d:SampleData ItemCount=5}" AutoGenerateColumns="False" CanUserResizeRows="False" ColumnWidth="SizeToCells" CanUserResizeColumns="False" CanUserReorderColumns="False" CanUserSortColumns="False" GridLinesVisibility="None" CellStyle="{StaticResource NoBorderCellStyle}" >
                        <DataGrid.Columns >
                            <DataGridCheckBoxColumn Width="auto" Header="{local:Loc EditLoadFilesWindow.CheckBox}" Binding="{Binding IsSelectedToRemove, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" >
                                <DataGridCheckBoxColumn.ElementStyle>
                                    <Style TargetType="CheckBox">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridCheckBoxColumn.ElementStyle>
                                <DataGridCheckBoxColumn.EditingElementStyle>
                                    <Style TargetType="CheckBox">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridCheckBoxColumn.EditingElementStyle>
                            </DataGridCheckBoxColumn>

                            <DataGridTemplateColumn Width="*" Header="{local:Loc EditLoadFilesWindow.File}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>

                                        <TextBlock d:Text="FileName" Text="{Binding Name}" Padding="10 0">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>

                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Width="auto" Header="{local:Loc EditLoadFilesWindow.Info}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="i" Click="ShowInfo_Click" FontWeight="DemiBold" FontStyle="Italic" FontSize="15" Width="40" Height="40" Background="Transparent" BorderThickness="0"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="{local:Loc EditLoadFilesWindow.Sourcefiles}" x:Name="Source">
                <Grid >
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="0,3">
                        <Button Padding="5 0" Tag="Source" Click="RemoveFile_Click" Content="{local:Loc EditLoadFilesWindow.Delete}" />
                        <Button Padding="5 0" Tag="Source" Click="AddFile_Click" Margin="2 0" Content="{local:Loc EditLoadFilesWindow.AddFile}" />
                    </StackPanel>

                    <DataGrid IsReadOnly="False" Grid.Row="1" HeadersVisibility="Column" ItemsSource="{Binding TemperarySourceFiles}" d:ItemsSource="{d:SampleData ItemCount=5}" AutoGenerateColumns="False" CanUserResizeRows="False" CanUserResizeColumns="False" CanUserReorderColumns="False" CanUserSortColumns="False" ColumnWidth="*" GridLinesVisibility="None" CellStyle="{StaticResource NoBorderCellStyle}" >
                        <DataGrid.Columns>
                            <DataGridCheckBoxColumn Header="{local:Loc EditLoadFilesWindow.CheckBox}" Width="auto" Binding="{Binding IsSelectedToRemove, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                <DataGridCheckBoxColumn.ElementStyle>
                                    <Style TargetType="CheckBox">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridCheckBoxColumn.ElementStyle>
                                <DataGridCheckBoxColumn.EditingElementStyle>
                                    <Style TargetType="CheckBox">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridCheckBoxColumn.EditingElementStyle>
                            </DataGridCheckBoxColumn>

                            <DataGridTemplateColumn Width="*" Header="{local:Loc EditLoadFilesWindow.File}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>

                                        <TextBlock d:Text="FileName" Text="{Binding Name}" Padding="10 0" ToolTip="{Binding Name}" >
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>

                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Width="auto" Header="{local:Loc EditLoadFilesWindow.Info}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="i" Click="ShowInfo_Click" FontWeight="DemiBold" FontStyle="Italic" FontSize="15" Width="40" Height="40" Background="Transparent" BorderThickness="0"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Left" Margin="76,3,0,3" Grid.ColumnSpan="2">
            <Button Content="{local:Loc EditLoadFilesWindow.Cancel}" Click="Close_Click" Padding="10 2" Margin="2 0" MinWidth="100"/>
            <Button Content="{local:Loc EditLoadFilesWindow.Apply}" Click="ApplyButton_Click" Padding="10 2" MinWidth="110"/>
        </StackPanel>
    </Grid>
</Window>
