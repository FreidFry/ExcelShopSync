<Window x:Class="ExcelShSy.Ui.MainWindow"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
       xmlns:resx="clr-namespace:ExcelShSy.Ui.Resources"
       mc:Ignorable="d"
       Title="ExcelShSy"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen"
        MinWidth="400" MinHeight="300" WindowStyle="ThreeDBorderWindow">

    <ScrollViewer HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Margin="10 3" Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center" >
                <!-- getTarget -->
                <Button Grid.Row="0" Grid.Column="0" Content="{x:Static resx:MainWindow.GetTargetFile}" Padding="5,0" Click="GetTargetFile_Click"/>
                <TextBlock x:Name="GetTargetFileLable" Text="Last file" Grid.Row="0" Grid.Column="1" Margin="10 0" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap" MaxWidth="250" HorizontalAlignment="Left"  VerticalAlignment="Center"/>
                <Button Grid.Row="0" Grid.Column="2" Content="{x:Static resx:MainWindow.EditFiles}" HorizontalAlignment="Left" VerticalAlignment="Center" Click="ShowEditLoadFiles_Click" Tag="Target" />
            </StackPanel>

            <StackPanel Margin="10 5" Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Center" >
                <!-- getSource -->
                <Button Grid.Row="1" Grid.Column="0" Content="{x:Static resx:MainWindow.GetSourceFile}" Padding="5,0" Click="GetSourceFile_Click"/>
                <TextBlock x:Name="GetSourceFileLable" Text="Last file" Grid.Row="1" Grid.Column="1" Margin="10 0" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap" MaxWidth="250" HorizontalAlignment="Left"  VerticalAlignment="Center"/>
                <Button Grid.Row="1" Grid.Column="2" Content="{x:Static resx:MainWindow.EditFiles}" HorizontalAlignment="Left" VerticalAlignment="Center" Click="ShowEditLoadFiles_Click" Tag="Source"/>
            </StackPanel>

            <Grid Grid.Row="2" Grid.ColumnSpan="4"  Margin="0" x:Name="TaskGrid">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- operations -->
                <CheckBox Grid.Row="1" Margin="10 2.5" Content="{x:Static resx:MainWindow.SyncAvailability}" Tag="SyncAvailability" Click="CheckBox_Click" VerticalAlignment="Center" />
                <CheckBox Grid.Row="2" Margin="10 2.5" Content="{x:Static resx:MainWindow.SyncQuantity}" Tag="SyncQuantity" Click="CheckBox_Click" VerticalAlignment="Center" />
                <CheckBox Grid.Row="3" Margin="10 2.5" Content="{x:Static resx:MainWindow.SyncDiscount}" Tag="SyncDiscount" Click="CheckBox_Click" VerticalAlignment="Center" />
                <CheckBox Grid.Row="4" Margin="10 2.5" Content="{x:Static resx:MainWindow.DiscountDate}" Tag="DiscountDate" Click="CheckBox_Click" VerticalAlignment="Center" />

                <StackPanel Margin="10, 2.5" Grid.Row="0" Orientation="Horizontal" >
                    <CheckBox Content="{x:Static resx:MainWindow.SyncPrice}" Tag="SyncPrice" Click="CheckBox_Click" VerticalAlignment="Center" />
                    <CheckBox Margin="5 0" Content="{x:Static resx:MainWindow.RoundUp}" Tag="IsRound" Click="CheckBox_Click" VerticalAlignment="Center" />
                    <CheckBox Content="{x:Static resx:MainWindow.IncreasePricePercent}" HorizontalAlignment="Left" Tag="IncreasePricePercent" Click="CheckBox_Click" VerticalAlignment="Center" />
                    <TextBox  Margin="5 0" TextWrapping="Wrap" Width="50" Name="IncreasePercentTextBox" TextChanged="ChangeIncreasePercent_TextChanged" PreviewTextInput="IncreasePercentTextBox_PreviewTextInput" DataObject.Pasting="IncreasePercentTextBox_Pasting" VerticalAlignment="Center" />

                </StackPanel>
                
            </Grid>
            <Button Grid.Row="3" x:Name="Setting" MinWidth="120" Content="{x:Static resx:MainWindow.Setting}" Margin="10" Click="SettingsWindowOpen_Click" HorizontalAlignment="Left" />
            <!-- start -->
            <Button Grid.Row="3" x:Name="ExecuteTasks" MinWidth="120" Content="{x:Static resx:MainWindow.ExecuteTasks}" Margin="10" Click="ExecuteTasks_Click" HorizontalAlignment="Right" />
        </Grid>
    </ScrollViewer>
</Window>
