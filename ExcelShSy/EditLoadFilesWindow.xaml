<Window x:Class="ExcelShSy.EditLoadFilesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExcelShSy"
        xmlns:converters="clr-namespace:ExcelShSy.Core.Extensions"
        mc:Ignorable="d"
        Title="Edit Load Files" SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen"
        MinWidth="250" ResizeMode="CanMinimize" MinHeight="400" MaxHeight="400">
    <Window.Resources>
        <converters:FileNameConverter x:Key="FileNameConverter"/>
    </Window.Resources>
    <Grid>
        <TabControl x:Name="FilesControl" SelectionChanged="TabControl_SelectionChanged">
            <TabItem Header="{Binding [TargetFiles], Source={StaticResource Loc}}" Width="Auto" Name="Target">
                <Grid Background="#FFE5E5E5" >
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <DataGrid Grid.Row="0" Grid.ColumnSpan="2" ItemsSource="{Binding TemperaryFile}" AutoGenerateColumns="False" CanUserResizeColumns="False" CanUserResizeRows="False" RowHeight="40" GridLinesVisibility="None" MinRowHeight="40" FrozenColumnCount="2" ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="VerticalAlignment" Value="Stretch"/>
                                <Setter Property="Height" Value="40"/>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                <Setter Property="VerticalContentAlignment" Value="Center"/>
                            </Style>
                        </DataGrid.CellStyle>
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="{Binding [File], Source={StaticResource Loc}}" Width="250" MinWidth="50">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding ., Converter={StaticResource FileNameConverter}}" 
                                            VerticalAlignment="Center" HorizontalAlignment="Center" 
                                            TextAlignment="Center" Height="40" Padding="10"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Width="Auto">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="i" Click="ShowInfoFile_Click" Padding="5" Width="40" Height="40" HorizontalAlignment="Right" Background="{x:Null}" BorderBrush="{x:Null}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Width="Auto">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="{Binding [Delete], Source={StaticResource Loc}}" Click="Delete_Click" Padding="5 0" Width="Auto" Height="40" HorizontalAlignment="Right" Background="{x:Null}" BorderBrush="{x:Null}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                    <Button Grid.Row="1" Grid.ColumnSpan="2" Width="Auto" Height="Auto" Click="AddFiles_Click" VerticalAlignment="Bottom" Margin="5" Content="{Binding [AddFile], Source={StaticResource Loc}}" FontSize="16"/>
                </Grid>
            </TabItem>



            <TabItem Header="{Binding [Sourcefiles], Source={StaticResource Loc}}" Width="Auto" Name="Source">
                <Grid Background="#FFE5E5E5" >
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <DataGrid Grid.Row="0" Grid.ColumnSpan="2" ItemsSource="{Binding TemperaryFile}" AutoGenerateColumns="False" CanUserResizeColumns="False" CanUserResizeRows="False" RowHeight="40" GridLinesVisibility="None" MinRowHeight="40" FrozenColumnCount="2" ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="VerticalAlignment" Value="Stretch"/>
                                <Setter Property="Height" Value="40"/>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                <Setter Property="VerticalContentAlignment" Value="Center"/>
                            </Style>
                        </DataGrid.CellStyle>
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="{Binding [File], Source={StaticResource Loc}}" Width="250" MinWidth="50">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding ., Converter={StaticResource FileNameConverter}}" 
                                            VerticalAlignment="Center" HorizontalAlignment="Center" 
                                            TextAlignment="Center" Height="40" Padding="10"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Width="Auto">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="i" Click="ShowInfoFile_Click" Padding="5" Width="40" Height="40" HorizontalAlignment="Right" Background="{x:Null}" BorderBrush="{x:Null}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Width="Auto">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="{Binding [Delete], Source={StaticResource Loc}}" Click="Delete_Click" Padding="5 0" Width="Auto" Height="40" HorizontalAlignment="Right" Background="{x:Null}" BorderBrush="{x:Null}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                    <Button Grid.Row="1" Grid.ColumnSpan="2" Width="Auto" Height="Auto" Click="AddFiles_Click" VerticalAlignment="Bottom" Margin="5" Content="{Binding [AddFile], Source={StaticResource Loc}}" FontSize="16"/>
                </Grid>
            </TabItem>
        </TabControl>

    </Grid>
</Window>
