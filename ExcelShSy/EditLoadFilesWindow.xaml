<Window x:Class="ExcelShSy.EditLoadFilesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExcelShSy"
        xmlns:converters="clr-namespace:ExcelShSy.Core.Extensions"
        mc:Ignorable="d"
        Title="Edit Load Files" SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen"
        MinWidth="250" MinHeight="400" ResizeMode="CanMinimize">
    <Window.Resources>
        <converters:FileNameConverter x:Key="FileNameConverter"/>
    </Window.Resources>
    <Grid>
        <TabControl x:Name="FilesControl" SelectionChanged="TabControl_SelectionChanged">
            <TabItem Header="{Binding [TargetFiles], Source={StaticResource Loc}}" Width="Auto" Name="Target">
                <Grid Background="#FFE5E5E5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <DataGrid ItemsSource="{Binding TemperaryFile}" AutoGenerateColumns="False" Grid.ColumnSpan="2" CanUserResizeColumns="False" CanUserResizeRows="False" RowHeight="40" GridLinesVisibility="None" MinRowHeight="40" FrozenColumnCount="2">
                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                <Setter Property="VerticalContentAlignment" Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGrid.CellStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="{Binding [File], Source={StaticResource Loc}}" Binding="{Binding Converter={StaticResource FileNameConverter}}" Width="250" MinWidth="50"/>
                            <DataGridTemplateColumn Header="{Binding [Delete], Source={StaticResource Loc}}" Width="Auto">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="{Binding [Delete], Source={StaticResource Loc}}" Click="Delete_Click" Padding="5 0" Width="Auto" HorizontalAlignment="Right" Background="{x:Null}" BorderBrush="{x:Null}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                    <Button Width="Auto" Height="Auto" Click="AddFiles_Click" VerticalAlignment="Bottom" Margin="5" Content="{Binding [AddFile], Source={StaticResource Loc}}" FontSize="16"/>
                </Grid>
            </TabItem>



            <TabItem Header="{Binding [Sourcefiles], Source={StaticResource Loc}}" Width="Auto" Name="Source">
                <Grid Background="#FFE5E5E5" AllowDrop="True">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <DataGrid ItemsSource="{Binding TemperaryFile}" AutoGenerateColumns="False" Grid.ColumnSpan="2"  CanUserResizeColumns="False" CanUserResizeRows="False" RowHeight="40" GridLinesVisibility="None" MinRowHeight="50" FrozenColumnCount="2">
                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                <Setter Property="VerticalContentAlignment" Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGrid.CellStyle>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="{Binding [File], Source={StaticResource Loc}}" Binding="{Binding Converter={StaticResource FileNameConverter}}" Width="250" MinWidth="50"/>
                            <DataGridTemplateColumn Header="{Binding [Delete], Source={StaticResource Loc}}" Width="Auto" >
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="{Binding [Delete], Source={StaticResource Loc}}" Click="Delete_Click" Padding="5 0" Width="Auto" HorizontalAlignment="Right" Background="{x:Null}" BorderBrush="{x:Null}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                    <Button Width="Auto" Height="Auto" Click="AddFiles_Click" VerticalAlignment="Bottom" Margin="5" Content="{Binding [AddFile], Source={StaticResource Loc}}" FontSize="16"/>
                </Grid>
            </TabItem>
        </TabControl>

    </Grid>
</Window>
